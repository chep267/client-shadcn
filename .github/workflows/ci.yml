name: CI/CD Pipeline - GitHub Pages

on:
    push:
        branches: [main, develop]
    pull_request:
        branches: [main]

permissions:
    contents: read
    pages: write
    id-token: write

concurrency:
    group: 'pages'
    cancel-in-progress: true

jobs:
    # --- GIAI ĐOẠN 1: CI ---
    ci:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v5

            - name: Setup pnpm
              uses: pnpm/action-setup@v5
              with:
                  version: 12

            - name: Setup Node.js
              uses: actions/setup-node@v5
              with:
                  node-version: 24
                  cache: 'pnpm'

            - name: Install Dependencies
              run: pnpm install --frozen-lockfile

            - name: Linter
              run: pnpm lint

            - name: Type check
              run: pnpm exec tsc --noEmit

            - name: Build Project
              run: pnpm build
              env:
                  # Đọc tự động các biến bạn đã add trên GitHub Settings (Variables / Secrets)
                  VITE_APP_API_HOST: ${{ vars.VITE_APP_API_HOST || secrets.VITE_APP_API_HOST }}
                  VITE_APP_CLIENT_HOST: ${{ vars.VITE_APP_CLIENT_HOST || secrets.VITE_APP_CLIENT_HOST }}
                  VITE_APP_CLIENT_PORT: ${{ vars.VITE_APP_CLIENT_PORT || secrets.VITE_APP_CLIENT_PORT }}
                  VITE_APP_CLIENT_LOCALE: ${{ vars.VITE_APP_CLIENT_LOCALE || secrets.VITE_APP_CLIENT_LOCALE }}
                  VITE_APP_CLIENT_THEME: ${{ vars.VITE_APP_CLIENT_THEME || secrets.VITE_APP_CLIENT_THEME }}
                  VITE_APP_BUILD_GZIP: ${{ vars.VITE_APP_BUILD_GZIP || secrets.VITE_APP_BUILD_GZIP }}
                  VITE_APP_PUSHER_KEY: ${{ vars.VITE_APP_PUSHER_KEY || secrets.VITE_APP_PUSHER_KEY }}
                  VITE_APP_PUSHER_CLUSTER: ${{ vars.VITE_APP_PUSHER_CLUSTER || secrets.VITE_APP_PUSHER_CLUSTER }}

            - name: Upload dist artifact
              uses: actions/upload-artifact@v4
              with:
                  name: build-dist
                  path: './dist'
                  retention-days: 1

    # --- GIAI ĐOẠN 2: CD ---
    cd:
        needs: ci
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        runs-on: ubuntu-latest

        environment:
            name: github-pages
            url: ${{ steps.deployment.outputs.page_url }}

        steps:
            - name: Download dist artifact
              uses: actions/download-artifact@v4
              with:
                  name: build-dist
                  path: './dist'

            - name: Setup Pages
              uses: actions/configure-pages@v5

            - name: Upload GitHub Pages artifact
              uses: actions/upload-pages-artifact@v4
              with:
                  path: './dist'

            - name: Deploy to GitHub Pages
              id: deployment
              uses: actions/deploy-pages@v5
